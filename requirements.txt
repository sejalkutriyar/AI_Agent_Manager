chromadb
langchain
langchain_openai
pandas
streamlit>=1.36.0
altair<5
google-generativeai
# Removed `pysqlite3-binary` because prebuilt wheels are unavailable on Windows/PyPI.
# The app falls back to the standard library `sqlite3` if `pysqlite3` isn't present.